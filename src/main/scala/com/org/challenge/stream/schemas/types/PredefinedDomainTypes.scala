package com.org.challenge.stream.schemas.types

/**
 * In this object we defined all the datatypes that can be processed by the current library.
 * If your use case requires more types, consider adding it in the same format as the existing ones.
 */
case object PredefinedDomainTypes {
  val String = "string"
  val Long = "long"
  val Short = "short"
  val Int = "int"
  val Timestamp = "timestamp"
}
